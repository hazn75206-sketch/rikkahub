name: Build APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: web-ui/pnpm-lock.yaml

      - name: Install web-ui dependencies
        working-directory: web-ui
        run: pnpm install --frozen-lockfile

      - name: Set up Gradle (with build cache)
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Create placeholder google-services.json
        run: |
          cat > app/google-services.json <<'EOF'
          {
            "project_info": {
              "project_number": "000000000000",
              "project_id": "placeholder-project",
              "storage_bucket": "placeholder-project.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:000000000000:android:0000000000000000000000",
                  "android_client_info": {
                    "package_name": "me.rerere.rikkahub"
                  }
                },
                "oauth_client": [],
                "api_key": [
                  {
                    "current_key": "AIzaSyAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              },
              {
                "client_info": {
                  "mobilesdk_app_id": "1:000000000000:android:0000000000000000000001",
                  "android_client_info": {
                    "package_name": "me.rerere.rikkahub.debug"
                  }
                },
                "oauth_client": [],
                "api_key": [
                  {
                    "current_key": "AIzaSyBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              }
            ],
            "configuration_version": "1"
          }
          EOF

      - name: Build Debug APK
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Verify bundled locales in APK
        run: |
          cd app/build/outputs/apk/debug
          unzip -p *.apk res/xml/locales_config.xml | head -30
          unzip -l *.apk | grep -E "values-id|values-zh|values-ja" | head -5 || echo "no per-locale files (compiled into resources.arsc)"

      - name: Rename APK
        run: |
          cd app/build/outputs/apk/debug
          for f in *.apk; do
            mv "$f" "rikkahub-arm64-debug.apk"
          done

      - name: Write APK link to job summary
        run: |
          echo "## Download APK" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Tanggal build: \`$(date -u +%Y-%m-%d %H:%M UTC)\`" >> $GITHUB_STEP_SUMMARY
          echo "Commit: \`${GITHUB_SHA::7}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "[klik di sini untuk download APK (rikkahub-arm64-debug.apk)](https://github.com/hazn75206-sketch/rikkahub/releases/download/apk-build/rikkahub-arm64-debug.apk)" >> $GITHUB_STEP_SUMMARY

      - name: Publish APK as Release asset (direct download)
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: apk-build
          name: RikkaHub APK (arm64)
          prerelease: true
          body: |
            Build otomatis dari commit `${{ github.sha }}` — tanggal: `${{ github.event.head_commit.timestamp }}`

            - APK: `rikkahub-arm64-debug.apk` (langsung download, bukan zip)
            - Download: https://github.com/hazn75206-sketch/rikkahub/releases/download/apk-build/rikkahub-arm64-debug.apk
          files: app/build/outputs/apk/debug/*.apk
          fail_on_unmatched_files: true
